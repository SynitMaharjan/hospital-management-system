@extends('layouts.dashboard')

@section('page-title')
  Doctor Dashboard
@endsection

@section('dashboard-content')
    <div class="container-fluid px-4">
        <!-- Header Section -->
        <div class="row mb-4">
            <div class="col-12">
                <div class="d-flex flex-column flex-md-row justify-content-between align-items-start align-items-md-center gap-3">
                    <div>
                        <h1 class="fw-bold mb-1">
                            <i class="bi bi-person-badge-fill text-primary me-2"></i>
                            Good {{ now()->hour < 12 ? 'Morning' : (now()->hour < 17 ? 'Afternoon' : 'Evening') }}, Dr. {{ Auth::user()->name }}
                        </h1>
                        <p class="text-muted mb-0">{{ now()->format('l, F j, Y') }} • {{ now()->format('g:i A') }}</p>
                    </div>
                    <div class="d-flex gap-2 flex-wrap">
                        <a href="{{ route('doctor.appointment.index') }}" class="btn btn-primary d-flex align-items-center gap-2">
                            <i class="bi bi-plus-circle"></i> New Appointment
                        </a>
                        <a href="{{ route('doctor.appointment.index') }}" class="btn btn-outline-secondary d-flex align-items-center gap-2">
                            <i class="bi bi-calendar-check"></i> View All
                        </a>
                    </div>
                </div>
            </div>
        </div>

        <!-- Stats Cards -->
        <div class="row g-3 mb-4">
            <div class="col-12 col-sm-6 col-lg-3">
                <div class="card stat-card h-100 border-0 shadow-sm">
                    <div class="card-body d-flex flex-column">
                        <div class="d-flex justify-content-between align-items-start">
                            <div>
                                <p class="text-muted small fw-medium mb-1">Today's Appointments</p>
                                <h2 class="fw-bold mb-0">{{ $todaysAppointmentCount }}</h2>
                            </div>
                            <div class="stat-icon bg-primary-subtle text-primary rounded-circle p-3">
                                <i class="bi bi-calendar2-day fs-3"></i>
                            </div>
                        </div>
                        <div class="mt-auto pt-2">
                            <small class="text-success d-flex align-items-center gap-1">
                                <i class="bi bi-check-circle-fill"></i> {{ $completedTodayCount }} completed
                            </small>
                        </div>
                    </div>
                </div>
            </div>

            <div class="col-12 col-sm-6 col-lg-3">
                <div class="card stat-card h-100 border-0 shadow-sm">
                    <div class="card-body d-flex flex-column">
                        <div class="d-flex justify-content-between align-items-start">
                            <div>
                                <p class="text-muted small fw-medium mb-1">Upcoming Appointments</p>
                                <h2 class="fw-bold mb-0">{{ $upcomingAppointmentCount }}</h2>
                            </div>
                            <div class="stat-icon bg-success-subtle text-success rounded-circle p-3">
                                <i class="bi bi-calendar2-plus fs-3"></i>
                            </div>
                        </div>
                        <div class="mt-auto pt-2">
                            <small class="text-info d-flex align-items-center gap-1">
                                <i class="bi bi-calendar2-week"></i> Next 7 days
                            </small>
                        </div>
                    </div>
                </div>
            </div>

            <div class="col-12 col-sm-6 col-lg-3">
                <div class="card stat-card h-100 border-0 shadow-sm">
                    <div class="card-body d-flex flex-column">
                        <div class="d-flex justify-content-between align-items-start">
                            <div>
                                <p class="text-muted small fw-medium mb-1">Total Patients</p>
                                <h2 class="fw-bold mb-0">{{ $totalPatients }}</h2>
                            </div>
                            <div class="stat-icon bg-info-subtle text-info rounded-circle p-3">
                                <i class="bi bi-people-fill fs-3"></i>
                            </div>
                        </div>
                        <div class="mt-auto pt-2">
                            <small class="text-muted d-flex align-items-center gap-1">
                                <i class="bi bi-person-plus"></i> Active this year
                            </small>
                        </div>
                    </div>
                </div>
            </div>

            <div class="col-12 col-sm-6 col-lg-3">
                <div class="card stat-card h-100 border-0 shadow-sm">
                    <div class="card-body d-flex flex-column">
                        <div class="d-flex justify-content-between align-items-start">
                            <div>
                                <p class="text-muted small fw-medium mb-1">This Week</p>
                                <h2 class="fw-bold mb-0">{{ $thisWeekCount ?? 0 }}</h2>
                            </div>
                            <div class="stat-icon bg-warning-subtle text-warning rounded-circle p-3">
                                <i class="bi bi-calendar2-week fs-3"></i>
                            </div>
                        </div>
                        <div class="mt-auto pt-2">
                            <small class="text-muted d-flex align-items-center gap-1">
                                <i class="bi bi-arrow-up-right"></i> vs last week
                            </small>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Main Content Grid -->
        <div class="row g-4">
            <!-- Today's Schedule -->
            <div class="col-12 col-lg-8">
                <div class="card h-100 border-0 shadow-sm">
                    <div class="card-header bg-white border-0 pb-0 d-flex justify-content-between align-items-center">
                        <div class="d-flex align-items-center gap-2">
                            <div class="bg-primary-subtle text-primary rounded-circle p-2">
                                <i class="bi bi-clock-history fs-4"></i>
                            </div>
                            <div>
                                <h5 class="fw-bold mb-0">Today's Schedule</h5>
                                <small class="text-muted">{{ $todayAppointments->count() }} appointment{{ $todayAppointments->count() !== 1 ? 's' : '' }}</small>
                            </div>
                        </div>
                        <a href="{{ route('doctor.appointment.index') }}" class="btn btn-sm btn-outline-primary">
                            <i class="bi bi-chevron-right"></i> View All
                        </a>
                    </div>
                    <div class="card-body p-0">
                        @if($todayAppointments->isEmpty())
                            <div class="text-center py-5">
                                <div class="empty-state mx-auto mb-3">
                                    <i class="bi bi-calendar-x fs-1 text-muted"></i>
                                </div>
                                <h6 class="fw-medium text-muted mb-1">No appointments today</h6>
                                <p class="text-muted small mb-3">Enjoy your free day!</p>
                                <a href="{{ route('doctor.appointment.index') }}" class="btn btn-sm btn-outline-primary">
                                    <i class="bi bi-calendar-plus"></i> Check Upcoming
                                </a>
                            </div>
                        @else
                            <div class="schedule-list">
                                @foreach($todayAppointments as $index => $appointment)
                                    <div class="schedule-item p-3 border-top {{ $index === 0 ? 'border-0' : '' }} {{ $appointment->status === 'completed' ? 'bg-light' : '' }} {{ $appointment->appointment_time < now()->format('H:i') && $appointment->status !== 'completed' ? 'bg-danger bg-opacity-10' : '' }}">
                                        <div class="d-flex gap-3 align-items-center">
                                            <!-- Time Column -->
                                            <div class="time-column text-center min-w-80px">
                                                <div class="fw-bold fs-5 {{ $appointment->status === 'completed' ? 'text-success' : ($appointment->appointment_time < now()->format('H:i') && $appointment->status !== 'completed' ? 'text-danger' : 'text-primary') }}">
                                                    {{ \Carbon\Carbon::parse($appointment->appointment_time)->format('g:i A') }}
                                                </div>
                                                <span class="badge {{ $appointment->status === 'completed' ? 'bg-success' : ($appointment->status === 'cancelled' ? 'bg-danger' : ($appointment->appointment_time < now()->format('H:i') && $appointment->status !== 'completed' ? 'bg-danger' : 'bg-warning')) }} rounded-pill px-2 py-1 small">
                                                    {{ ucfirst($appointment->status) }}
                                                </span>
                                            </div>

                                            <!-- Patient Info -->
                                            <div class="patient-info flex-grow-1 min-w-0">
                                                <div class="d-flex align-items-center gap-2 mb-1">
                                                    @if($appointment->patient->user)
                                                        <img src="{{ $appointment->patient->user->profile_picture_url }}" alt="{{ $appointment->patient->user->name }}" class="rounded-circle" width="36" height="36">
                                                    @else
                                                        <div class="rounded-circle bg-primary-subtle d-flex align-items-center justify-content-center text-primary" style="width: 36px; height: 36px;">
                                                            <i class="bi bi-person-fill"></i>
                                                        </div>
                                                    @endif
                                                    <div>
                                                        <h6 class="fw-medium mb-0 text-truncate">{{ $appointment->patient->user?->name ?? 'Unknown Patient' }}</h6>
                                                        <small class="text-muted">{{ $appointment->patient->phone ?? 'No phone' }}</small>
                                                    </div>
                                                </div>
                                                <div class="d-flex flex-wrap gap-2 small text-muted">
                                                    @if($appointment->reason)
                                                        <span class="d-flex align-items-center gap-1">
                                                            <i class="bi bi-chat-left-text"></i> {{ Str::limit($appointment->reason, 50) }}
                                                        </span>
                                                    @endif
                                                    @if($appointment->department)
                                                        <span class="d-flex align-items-center gap-1">
                                                            <i class="bi bi-building"></i> {{ $appointment->department->name }}
                                                        </span>
                                                    @endif
                                                </div>
                                            </div>

                                            <!-- Actions -->
                                            <div class="action-buttons d-flex gap-1">
                                                @if($appointment->status !== 'completed' && $appointment->status !== 'cancelled')
                                                    <a href="{{ route('doctor.appointment.show', $appointment) }}" class="btn btn-sm btn-primary" title="View Details">
                                                        <i class="bi bi-eye"></i>
                                                    </a>
                                                    <button type="button" class="btn btn-sm btn-outline-success" onclick="completeAppointment({{ $appointment->id }})" title="Mark Complete">
                                                        <i class="bi bi-check-lg"></i>
                                                    </button>
                                                @else
                                                    <a href="{{ route('doctor.appointment.show', $appointment) }}" class="btn btn-sm btn-outline-secondary" title="View Details">
                                                        <i class="bi bi-eye"></i>
                                                    </a>
                                                @endif
                                            </div>
                                        </div>
                                    </div>
                                @endforeach
                            </div>
                        @endif
                    </div>
                </div>
            </div>

            <!-- Sidebar: Quick Actions & Upcoming -->
            <div class="col-12 col-lg-4">
                <!-- Quick Actions -->
                <div class="card border-0 shadow-sm mb-4 h-100">
                    <div class="card-header bg-white border-0 pb-0">
                        <div class="d-flex align-items-center gap-2">
                            <div class="bg-success-subtle text-success rounded-circle p-2">
                                <i class="bi bi-lightning-charge fs-4"></i>
                            </div